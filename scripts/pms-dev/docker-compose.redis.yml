services:
  redis:
    image: redis:7-alpine
    container_name: pms_redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - pms_redis_data:/data
volumes:
  pms_redis_data:
