# 2026-02-27

## PMS Work
- Fixed extensive TypeScript and logic errors in `tenant_portal_backend`:
    - `maintenance.service.ts`: Fixed DTO type narrowing.
    - `rent-optimization.service.ts`: Fixed ID types (UUID vs Number), property access on relations, removed invalid `any` casts, and fixed schema mismatches (e.g., `propertyType` vs `type`).
    - `payments.service.ts`: Fixed `parseLeaseId` to handle UUID strings instead of numbers.
    - `quickbooks.service.ts`: Fixed incorrect schema usage (userId on Property, etc.), fixed date fields, corrected relational queries, and improved sync return types.
    - Verified `rental-application.service.ts` and `schedule.service.ts` compliance with Prisma schema.

## Pending
- Run build/lint check to verify all fixes.
- Next steps: `rent-estimator`, `rent-optimization`, `rental-application`, `schedule` are now technically "Finished" in terms of syntax repair.
