# Run Pack — T-20260215-d1a74284080c

## Objective
Tenant can complete MOVE_IN/MOVE_OUT inspections from `/tenant/inspections/:id` by filling checklist items (**requiresAction + notes only**), then submit to lock.

## Planned Runs

### RUN 1 — Backend (enable tenant edit: requiresAction + notes)
- jobKey: `tenant-inspections-edit-backend`
- branch: `ops`
- dryRun: false
- script: `scripts/pms/tenant-inspections-edit-backend.mjs`

Deliverables:
- Allow `TENANT` role on `PATCH /api/inspections/rooms/:roomId/items`.
- Tenant guardrails (server-side):
  - tenant has `Lease.status = ACTIVE`
  - inspection unit matches tenant lease unit
  - inspection type ∈ { MOVE_IN, MOVE_OUT }
  - inspection status != COMPLETED
- Only allow edits to: `requiresAction`, `notes`.

### RUN 2 — Frontend (tenant edit UI + save + lock)
- jobKey: `tenant-inspections-edit-frontend`
- branch: `ops`
- dryRun: false
- script: `scripts/pms/tenant-inspections-edit-frontend.mjs`

Deliverables:
- `/tenant/inspections/:id` renders rooms + checklist items.
- Tenant can edit `requiresAction` + `notes` and save (bulk per-room save).
- Tenant can press **Mark Completed**; once completed:
  - editing UI is locked/read-only
  - save calls are blocked

## Acceptance
- Tenant can edit + save `requiresAction` and `notes` on their MOVE_IN/MOVE_OUT inspection.
- Refresh persists edits.
- Tenant cannot edit after completion (API denies + UI blocks).
- Tenant cannot edit other unit’s inspection or other inspection types.

## Rollback / Fallback
- Revert the two commits for backend and frontend runs.
- If tenant edit endpoints become problematic: disable tenant role on the controller routes.
